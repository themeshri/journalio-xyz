// HeroUI Tailwind v4 plugin config (loaded via @plugin in app/globals.css).
// Stock default theme per the pilot decision — Journalio's emerald is NOT wired
// in, so HeroUI-rendered surfaces (currently just /settings) look like default
// HeroUI, intentionally distinct from the shadcn pages.
import { heroui } from '@heroui/react'

export default heroui()
