import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Missed Trades | Journalio',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
